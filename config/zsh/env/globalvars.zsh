#!/usr/bin/zsh

export EDITOR=vim

# The Grimoire Obsidian vault lives at the same path on every machine.
# (`cdg` in alias.zsh jumps there; init/fs.zsh creates the parent directory.)
export GRIMOIRE_DIR="$HOME/docs/obsidian_vaults/grimoire"
