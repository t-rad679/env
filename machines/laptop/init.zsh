#!/usr/bin/zsh

# Laptop-only init, sourced last by init/init.zsh when MACHINE_ROLE=laptop
# (also in --links-only mode, so keep everything here idempotent).
#
# Nothing yet. Power/backlight/touchpad tooling would go here (and in
# machines/laptop/packages.txt, machines/laptop/i3/) when it's wanted.
