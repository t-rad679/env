# Laptop-only i3 config. Included from config/i3/config via $MACHINE_ROLE.
#
# Intentionally empty for now: the laptop keeps i3/X defaults (so the display
# can blank and sleep) and has no role-specific autostarts yet. This file
# exists so the machines/laptop/i3/*.conf include always matches something.
