# Machine role for this box, read by zsh (config/zsh/.zshrc), the X session
# (config/.xprofile) and i3. Plain KEY=VALUE so sh, zsh and EnvironmentFile=
# consumers can all read it. Linked to ~/.config/machine-role.env by init/role.zsh.
MACHINE_ROLE=laptop
