# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!
#
# Split into conf.d/: baseconfig.conf (appearance, bindings, workspaces) and
# autostart.conf (startup programs + the workspace assignments that route
# them). Each conf.d file is self-contained, so include order doesn't matter.

include conf.d/*.conf
